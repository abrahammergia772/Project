# Test-only dependencies. Not installed on the deployed service.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# pglast matters: tests/test_schema.py uses PostgreSQL's REAL parser to check
# the Supabase migrations against the SQLAlchemy models. Without it those 36
# tests SKIP silently rather than fail, so schema drift slips through
# unnoticed. Keep it installed in CI.
-r requirements.txt

pytest==9.0.3
pglast==8.4
