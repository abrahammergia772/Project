fastapi==0.115.6
# starlette is pinned: FastAPI 0.115.x is not compatible with starlette >= 1.x,
# where include_router() silently mounts sub-apps instead of registering routes.
starlette==0.41.3
uvicorn[standard]==0.52.0
sqlalchemy==2.0.51
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
email-validator==2.3.0
groq==1.6.0
supabase==2.31.0
httpx==0.28.1

# Reading uploaded .xlsx attendance sheets. Small (~250 KB) and pure Python.
# CSV needs nothing extra; without this, .xlsx uploads return a clear 503
# telling the user to save as CSV rather than failing obscurely.
openpyxl==3.1.5
