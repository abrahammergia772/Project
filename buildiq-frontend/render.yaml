services:
  - type: web
    name: buildiq-backend
    env: python
    rootDir: buildiq-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ORIGINS
        value: https://cmsai.onrender.com
