# Superseded — see ../render.yaml at the REPO ROOT.
#
# Render only reads render.yaml from the repository root, so the Blueprint for
# BOTH services lives there and sets `rootDir` per service. Keeping a second
# copy here caused confusion (and a failed deploy: the build ran from the repo
# root, where requirements.txt does not exist).
#
# To deploy: New -> Blueprint -> pick this repo. Render finds /render.yaml and
# creates buildiq-api and buildiq-frontend together.
